type User {
  name: String
  id: String
  valance: Int
}

type Query {
  hello(name: String): User!
}

type Mutation {
  createUser(name: String!): User!
  changeValance(
    senderId: String!,
    recieverId: String!,
    amount: Int!
  ): Int!
}

