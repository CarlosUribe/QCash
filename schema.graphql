type Mutation {
  createUser(name: String!): User!
  changeBalance(senderId: String!, recieverId: String!, amount: Int!): Int!
}

type Query {
  hello(name: String): User!
}

type User {
  name: String
  id: String
  balance: Int
}

